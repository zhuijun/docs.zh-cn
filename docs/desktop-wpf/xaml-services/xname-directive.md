---
title: x:Name 指令
ms.date: 03/30/2017
f1_keywords:
- x:Name
- xName
- Name
helpviewer_keywords:
- x:Name attribute [XAML Services]
- XAML [XAML Services], x:Name attribute
- Name attribute in XAML [XAML Services]
ms.assetid: b7e61222-e8cf-48d2-acd0-6df3b7685d48
ms.openlocfilehash: ddaa35b18d1c632fc49b18dabf0d992dc1c78fcc
ms.sourcegitcommit: 27a15a55019f6b5f2733961738babe94aec0def3
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 09/15/2020
ms.locfileid: "90549478"
---
# <a name="xname-directive"></a><span data-ttu-id="caac5-102">x:Name 指令</span><span class="sxs-lookup"><span data-stu-id="caac5-102">x:Name Directive</span></span>

<span data-ttu-id="caac5-103">在 XAML 名称范围中唯一标识 XAML 定义的元素。</span><span class="sxs-lookup"><span data-stu-id="caac5-103">Uniquely identifies XAML-defined elements in a XAML namescope.</span></span> <span data-ttu-id="caac5-104">当框架提供 Api 或实现在运行时访问 XAML 创建的对象图的行为时，可以将 XAML 名称范围及其唯一性模型应用于实例化的对象。</span><span class="sxs-lookup"><span data-stu-id="caac5-104">XAML namescopes and their uniqueness models can be applied to the instantiated objects, when frameworks provide APIs or implement behaviors that access the XAML-created object graph at run time.</span></span>

## <a name="xaml-attribute-usage"></a><span data-ttu-id="caac5-105">XAML 属性用法</span><span class="sxs-lookup"><span data-stu-id="caac5-105">XAML Attribute Usage</span></span>

```xaml
<object x:Name="XAMLNameValue".../>
```

## <a name="xaml-values"></a><span data-ttu-id="caac5-106">XAML 值</span><span class="sxs-lookup"><span data-stu-id="caac5-106">XAML Values</span></span>

|||
|-|-|
|`XAMLNameValue`|<span data-ttu-id="caac5-107">符合 [XamlName 语法](xamlname-grammar.md)限制的字符串。</span><span class="sxs-lookup"><span data-stu-id="caac5-107">A string that conforms to the restrictions of the [XamlName Grammar](xamlname-grammar.md).</span></span>|

## <a name="remarks"></a><span data-ttu-id="caac5-108">备注</span><span class="sxs-lookup"><span data-stu-id="caac5-108">Remarks</span></span>

<span data-ttu-id="caac5-109">`x:Name`将应用于框架的后备编程模型后，该名称等效于保存构造函数返回的对象引用或实例的变量。</span><span class="sxs-lookup"><span data-stu-id="caac5-109">After `x:Name` is applied to a framework's backing programming model, the name is equivalent to the variable that holds an object reference or an instance as returned by a constructor.</span></span>

<span data-ttu-id="caac5-110">指令用法的值在 `x:Name` XAML 名称范围内必须是唯一的。</span><span class="sxs-lookup"><span data-stu-id="caac5-110">The value of an `x:Name` directive usage must be unique within a XAML namescope.</span></span> <span data-ttu-id="caac5-111">默认情况下，当由 .NET XAML 服务 API 使用时，主 XAML 名称范围将在单个 XAML 生产的 XAML 根元素上定义，并且包含该 XAML 生产中包含的元素。</span><span class="sxs-lookup"><span data-stu-id="caac5-111">By default when used by .NET XAML Services API, the primary XAML namescope is defined at the XAML root element of a single XAML production, and encompasses the elements that are contained in that XAML production.</span></span> <span data-ttu-id="caac5-112">可能出现在单个 XAML 生产内的其他离散 XAML 名称范围可以由框架定义，以解决特定方案。</span><span class="sxs-lookup"><span data-stu-id="caac5-112">Additional discrete XAML namescopes that might occur within a single XAML production can be defined by frameworks to address specific scenarios.</span></span> <span data-ttu-id="caac5-113">例如，在 WPF 中，新的 XAML 名称范围由也在该 XAML 生产环境中定义的任何模板来定义和创建。</span><span class="sxs-lookup"><span data-stu-id="caac5-113">For example, in WPF, new XAML namescopes are defined and created by any template that is also defined on that XAML production.</span></span> <span data-ttu-id="caac5-114">有关 XAML 名称范围 (为 WPF 编写但) 许多 XAML 名称范围概念相关的详细信息，请参阅 [WPF Xaml 名称范围](/dotnet/desktop/wpf/advanced/wpf-xaml-namescopes)。</span><span class="sxs-lookup"><span data-stu-id="caac5-114">For more information about XAML namescopes (written for WPF but relevant for many XAML namescope concepts), see [WPF XAML Namescopes](/dotnet/desktop/wpf/advanced/wpf-xaml-namescopes).</span></span>

<span data-ttu-id="caac5-115">通常， `x:Name` 不应在也使用的情况下应用 `x:Key` 。</span><span class="sxs-lookup"><span data-stu-id="caac5-115">In general, `x:Name` should not be applied in situations that also use `x:Key`.</span></span> <span data-ttu-id="caac5-116">由特定现有框架实现的 XAML 实现在和之间引入了替代概念 `x:Key` `x:Name` ，但这不是建议的做法。</span><span class="sxs-lookup"><span data-stu-id="caac5-116">XAML implementations by specific existing frameworks have introduced substitution concepts between `x:Key` and `x:Name`, but that is not a recommended practice.</span></span> <span data-ttu-id="caac5-117">在处理名称/键信息（如或）时，.NET XAML 服务不支持这种替换概念 <xref:System.Windows.Markup.INameScope> <xref:System.Windows.Markup.DictionaryKeyPropertyAttribute> 。</span><span class="sxs-lookup"><span data-stu-id="caac5-117">.NET XAML Services does not support such substitution concepts when handling name/key information such as <xref:System.Windows.Markup.INameScope> or <xref:System.Windows.Markup.DictionaryKeyPropertyAttribute>.</span></span>

<span data-ttu-id="caac5-118">Permittance 的规则 `x:Name` 和名称唯一性强制可能由特定的实现框架定义。</span><span class="sxs-lookup"><span data-stu-id="caac5-118">Rules for permittance of `x:Name` as well as the name uniqueness enforcement are potentially defined by specific implementing frameworks.</span></span> <span data-ttu-id="caac5-119">但是，若要与 .NET XAML 服务一起使用，XAML 名称范围唯一性的框架定义应该与 <xref:System.Windows.Markup.INameScope> 本文档中的信息定义一致，并且应使用与信息应用位置相同的规则。</span><span class="sxs-lookup"><span data-stu-id="caac5-119">However, to be usable with .NET XAML Services, the framework definitions of XAML namescope uniqueness should be consistent with the definition of <xref:System.Windows.Markup.INameScope> information in this documentation, and should use the same rules regarding where the information is applied.</span></span> <span data-ttu-id="caac5-120">例如，实现将 [!INCLUDE[TLA#tla_winclient](../../../includes/tlasharptla-winclient-md.md)] 各种标记元素分成不同的 <xref:System.Windows.NameScope> 范围，如资源字典、页面级别 XAML 创建的逻辑树、模板和其他延迟的内容，然后在每个 xaml 名称范围内强制执行 xaml 名称的唯一性。</span><span class="sxs-lookup"><span data-stu-id="caac5-120">For example, the [!INCLUDE[TLA#tla_winclient](../../../includes/tlasharptla-winclient-md.md)] implementation divides various markup elements into separate <xref:System.Windows.NameScope> ranges, such as resource dictionaries, the logical tree created by the page-level XAML, templates, and other deferred content, and then enforces XAML name uniqueness within each of those XAML namescopes.</span></span>

<span data-ttu-id="caac5-121">对于使用 .NET XAML 服务 XAML 对象编写器的自定义类型， `x:Name` 可以建立或更改映射到类型的的属性。</span><span class="sxs-lookup"><span data-stu-id="caac5-121">For custom types that use .NET XAML Services XAML object writers, a property that maps to `x:Name` on a type can be established or changed.</span></span> <span data-ttu-id="caac5-122">通过 <xref:System.Windows.Markup.RuntimeNamePropertyAttribute> 在类型定义代码中引用要映射的属性的名称来定义此行为。</span><span class="sxs-lookup"><span data-stu-id="caac5-122">You define this behavior by referencing the name of the property to map with the <xref:System.Windows.Markup.RuntimeNamePropertyAttribute> in the type definition code.</span></span>  <span data-ttu-id="caac5-123"><xref:System.Windows.Markup.RuntimeNamePropertyAttribute> 是一个类型级特性。</span><span class="sxs-lookup"><span data-stu-id="caac5-123"><xref:System.Windows.Markup.RuntimeNamePropertyAttribute> is a type-level attribute.</span></span>

<span data-ttu-id="caac5-124">Using.NET XAML 服务，可通过实现接口以非特定于框架的方式定义 XAML 名称范围支持的支持逻辑 <xref:System.Windows.Markup.INameScope> 。</span><span class="sxs-lookup"><span data-stu-id="caac5-124">Using.NET XAML Services, the backing logic for XAML namescope support can be defined in a framework-neutral way by implementing the <xref:System.Windows.Markup.INameScope> interface.</span></span>

## <a name="wpf-usage-notes"></a><span data-ttu-id="caac5-125">WPF 用法说明</span><span class="sxs-lookup"><span data-stu-id="caac5-125">WPF Usage Notes</span></span>

<span data-ttu-id="caac5-126">在 [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] 使用 XAML、分部类和代码隐藏的应用程序的标准生成配置下，指定的 `x:Name` 将成为当由标记编译生成任务处理时在基础代码中创建的字段的名称 [!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)] ，并且该字段包含对该对象的引用。</span><span class="sxs-lookup"><span data-stu-id="caac5-126">Under the standard build configuration for a [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] application that uses XAML, partial classes, and code-behind, the specified `x:Name` becomes the name of a field that is created in the underlying code when [!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)] is processed by a markup compilation build task, and that field holds a reference to the object.</span></span> <span data-ttu-id="caac5-127">默认情况下，创建的字段为内部字段。</span><span class="sxs-lookup"><span data-stu-id="caac5-127">By default, the created field is internal.</span></span> <span data-ttu-id="caac5-128">可以通过指定 [x:FieldModifier 属性](xfieldmodifier-directive.md)来更改字段访问权限。</span><span class="sxs-lookup"><span data-stu-id="caac5-128">You can change field access by specifying the [x:FieldModifier attribute](xfieldmodifier-directive.md).</span></span> <span data-ttu-id="caac5-129">在 WPF 和 Silverlight 中，序列是标记编译定义并命名分部类中的字段，但该值最初为空。</span><span class="sxs-lookup"><span data-stu-id="caac5-129">In WPF and Silverlight, the sequence is that the markup compile defines and names the field in a partial class, but the value is initially empty.</span></span> <span data-ttu-id="caac5-130">然后， `InitializeComponent` 从类构造函数中调用一个名为的生成方法。</span><span class="sxs-lookup"><span data-stu-id="caac5-130">Then, a generated method named `InitializeComponent` is called from within the class constructor.</span></span> <span data-ttu-id="caac5-131">`InitializeComponent` 包含 `FindName` 使用每个 `x:Name` 值的调用，此分部类的 XAML 定义部分中存在这些值作为输入字符串。</span><span class="sxs-lookup"><span data-stu-id="caac5-131">`InitializeComponent` consists of `FindName` calls using each of the `x:Name` values that exist in the XAML-defined part of the partial class as input strings.</span></span> <span data-ttu-id="caac5-132">然后，将返回值分配给赞命名字段引用，以用通过 XAML 分析创建的对象填充字段值。</span><span class="sxs-lookup"><span data-stu-id="caac5-132">The return values are then assigned to the like-named field reference to fill the field values with objects that were created from XAML parsing.</span></span> <span data-ttu-id="caac5-133">执行 `InitializeComponent` 时，可以直接使用/字段名称引用运行时对象关系图 `x:Name` ，而不必 `FindName` 在每次需要引用 XAML 定义的对象时都显式调用。</span><span class="sxs-lookup"><span data-stu-id="caac5-133">The execution of `InitializeComponent` make it possible to reference the run time object graph using the `x:Name` / field name directly, rather than having to call `FindName` explicitly any time you need a reference to a XAML-defined object.</span></span>

<span data-ttu-id="caac5-134">对于使用 Microsoft Visual Basic 目标的 WPF 应用程序，并包括包含生成操作的 XAML 文件 `Page` ，在编译过程中将创建一个单独的引用属性，该属性将 `WithEvents` 关键字添加到具有的所有元素 `x:Name` ，以支持 `Handles` 事件处理程序委托的语法。</span><span class="sxs-lookup"><span data-stu-id="caac5-134">For a WPF application that uses the Microsoft Visual Basic targets and includes XAML files with `Page` build action, a separate reference property is created during compilation that adds the `WithEvents` keyword to all elements that have an `x:Name`, to support `Handles` syntax for event handler delegates.</span></span> <span data-ttu-id="caac5-135">此属性始终是公共的。</span><span class="sxs-lookup"><span data-stu-id="caac5-135">This property is always public.</span></span> <span data-ttu-id="caac5-136">有关详细信息，请参阅 [Visual Basic 和 WPF 事件处理](/dotnet/desktop/wpf/advanced/visual-basic-and-wpf-event-handling)。</span><span class="sxs-lookup"><span data-stu-id="caac5-136">For more information, see [Visual Basic and WPF Event Handling](/dotnet/desktop/wpf/advanced/visual-basic-and-wpf-event-handling).</span></span>

<span data-ttu-id="caac5-137">`x:Name` 在加载时，WPF XAML 处理器使用将名称注册到 XAML 名称范围，即使该页未通过生成操作进行标记编译 (例如，) 的资源字典的松散 XAML。</span><span class="sxs-lookup"><span data-stu-id="caac5-137">`x:Name` is used by the WPF XAML processor to register a name into a XAML namescope at load time, even for cases where the page is not markup-compiled by build actions (for example, loose XAML of a resource dictionary).</span></span> <span data-ttu-id="caac5-138">此行为的一个原因是， `x:Name` 绑定可能需要 <xref:System.Windows.Data.Binding.ElementName%2A> 。</span><span class="sxs-lookup"><span data-stu-id="caac5-138">One reason for this behavior is because the `x:Name` is potentially needed for <xref:System.Windows.Data.Binding.ElementName%2A> binding.</span></span> <span data-ttu-id="caac5-139">有关详细信息，请参阅 [数据绑定概述](../data/data-binding-overview.md)。</span><span class="sxs-lookup"><span data-stu-id="caac5-139">For details, see [Data Binding Overview](../data/data-binding-overview.md).</span></span>

<span data-ttu-id="caac5-140">如前所述， `x:Name` `Name` 在也使用的情况下，不应应用 (或) `x:Key` 。</span><span class="sxs-lookup"><span data-stu-id="caac5-140">As mentioned previously, `x:Name` (or `Name`) should not be applied in situations that also use `x:Key`.</span></span> <span data-ttu-id="caac5-141">[!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] <xref:System.Windows.ResourceDictionary> 具有将自身定义为 XAML 名称范围、返回未实现的或 null 值 <xref:System.Windows.Markup.INameScope> 作为执行此行为的方法的特殊行为。</span><span class="sxs-lookup"><span data-stu-id="caac5-141">The [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] <xref:System.Windows.ResourceDictionary> has a special behavior of defining itself as a XAML namescope but returning Not Implemented or null values for <xref:System.Windows.Markup.INameScope> APIs as a way to enforce this behavior.</span></span> <span data-ttu-id="caac5-142">如果 WPF XAML 分析器在 `Name` xaml 定义中遇到或 `x:Name` <xref:System.Windows.ResourceDictionary> ，则不会将该名称添加到任何 xaml 名称范围。</span><span class="sxs-lookup"><span data-stu-id="caac5-142">If the WPF XAML parser encounters `Name` or `x:Name` in a XAML-defined <xref:System.Windows.ResourceDictionary>, the name is not added to any XAML namescope.</span></span> <span data-ttu-id="caac5-143">尝试从任何 XAML 名称范围和方法中查找该名称 `FindName` 将不会返回有效的结果。</span><span class="sxs-lookup"><span data-stu-id="caac5-143">Attempting to find that name from any XAML namescope and the `FindName` methods will not return valid results.</span></span>

### <a name="xname-and-name"></a><span data-ttu-id="caac5-144">X：Name 和名称</span><span class="sxs-lookup"><span data-stu-id="caac5-144">x:Name and Name</span></span>

<span data-ttu-id="caac5-145">很多 WPF 应用程序方案都可以避免使用 `x:Name` 特性，因为在 `Name` 默认 XAML 命名空间中为多个重要基类（如）指定的依赖项属性 <xref:System.Windows.FrameworkElement> <xref:System.Windows.FrameworkContentElement> 满足此相同目的。</span><span class="sxs-lookup"><span data-stu-id="caac5-145">Many WPF application scenarios can avoid any use of the `x:Name` attribute, because the `Name` dependency property as specified in the default XAML namespace for several of the important base classes such as <xref:System.Windows.FrameworkElement> and <xref:System.Windows.FrameworkContentElement> satisfies this same purpose.</span></span> <span data-ttu-id="caac5-146">还有一些常见 XAML 和 WPF 方案，在这些方案中，代码访问 `Name` 框架级别上没有属性的元素是非常重要的。</span><span class="sxs-lookup"><span data-stu-id="caac5-146">There are still some common XAML and WPF scenarios where code access to an element with no `Name` property at the framework level is important.</span></span> <span data-ttu-id="caac5-147">例如，某些动画和情节提要支持类不支持 `Name` 属性，但通常需要在代码中引用这些类才能控制动画。</span><span class="sxs-lookup"><span data-stu-id="caac5-147">For example, certain animation and storyboard support classes do not support a `Name` property, but they often need to be referenced in code in order to control the animation.</span></span> <span data-ttu-id="caac5-148">`x:Name`如果打算稍后从代码中引用它们，则应将指定为在 XAML 中创建的时间线和转换的属性。</span><span class="sxs-lookup"><span data-stu-id="caac5-148">You should specify `x:Name` as an attribute on timelines and transforms that are created in XAML, if you intend to reference them from code later.</span></span>

<span data-ttu-id="caac5-149">如果在 <xref:System.Windows.FrameworkElement.Name%2A> 类上作为属性提供， <xref:System.Windows.FrameworkElement.Name%2A> 并且 `x:Name` 可作为属性互换使用，但如果在同一元素上同时指定了这两个异常，则会导致分析异常。</span><span class="sxs-lookup"><span data-stu-id="caac5-149">If <xref:System.Windows.FrameworkElement.Name%2A> is available as a property on the class, <xref:System.Windows.FrameworkElement.Name%2A> and `x:Name` can be used interchangeably as attributes, but a parse exception will result if both are specified on the same element.</span></span> <span data-ttu-id="caac5-150">如果 XAML 是标记编译的，则异常将在标记编译中发生，否则会在加载时发生。</span><span class="sxs-lookup"><span data-stu-id="caac5-150">If the XAML is markup compiled, the exception will occur on the markup compile, otherwise it occurs on load.</span></span>

<span data-ttu-id="caac5-151"><xref:System.Windows.FrameworkElement.Name%2A> 可以使用 XAML 特性语法和使用中的代码进行设置 <xref:System.Windows.DependencyObject.SetValue%2A> 。请注意，在代码中设置该 <xref:System.Windows.FrameworkElement.Name%2A> 属性不会在已加载 xaml 的大多数情况下在 xaml 名称范围内创建代表字段引用。</span><span class="sxs-lookup"><span data-stu-id="caac5-151"><xref:System.Windows.FrameworkElement.Name%2A> can be set using XAML attribute syntax, and in code using <xref:System.Windows.DependencyObject.SetValue%2A>; note however that setting the <xref:System.Windows.FrameworkElement.Name%2A> property in code does not create the representative field reference within the XAML namescope in most circumstances where the XAML is already loaded.</span></span> <span data-ttu-id="caac5-152">不要尝试 <xref:System.Windows.FrameworkElement.Name%2A> 在代码中进行设置，而是 <xref:System.Windows.NameScope> 根据相应的名称范围使用代码中的方法。</span><span class="sxs-lookup"><span data-stu-id="caac5-152">Instead of attempting to set <xref:System.Windows.FrameworkElement.Name%2A> in code, use <xref:System.Windows.NameScope> methods from code, against the appropriate namescope.</span></span>

<span data-ttu-id="caac5-153"><xref:System.Windows.FrameworkElement.Name%2A> 还可以通过对内部文本使用属性元素语法来设置，但这种情况并不常见。</span><span class="sxs-lookup"><span data-stu-id="caac5-153"><xref:System.Windows.FrameworkElement.Name%2A> can also be set using property element syntax with inner text, but that is uncommon.</span></span> <span data-ttu-id="caac5-154">相反， `x:Name` 不能在 XAML 属性元素语法中或使用代码中设置 <xref:System.Windows.DependencyObject.SetValue%2A> ; 它只能使用对象的属性语法进行设置，因为它是一个指令。</span><span class="sxs-lookup"><span data-stu-id="caac5-154">In contrast, `x:Name` cannot be set in XAML property element syntax, or in code using <xref:System.Windows.DependencyObject.SetValue%2A>; it can only be set using attribute syntax on objects because it is a directive.</span></span>

## <a name="silverlight-usage-notes"></a><span data-ttu-id="caac5-155">Silverlight 使用说明</span><span class="sxs-lookup"><span data-stu-id="caac5-155">Silverlight Usage Notes</span></span>

<span data-ttu-id="caac5-156">`x:Name` 对于 Silverlight，单独记录。</span><span class="sxs-lookup"><span data-stu-id="caac5-156">`x:Name` for Silverlight is documented separately.</span></span> <span data-ttu-id="caac5-157">有关详细信息，请参阅 [XAML 命名空间 (x： ) 语言功能 (Silverlight) ](/previous-versions/windows/silverlight/dotnet-windows-silverlight/cc188995(v=vs.95))。</span><span class="sxs-lookup"><span data-stu-id="caac5-157">For more information, see [XAML Namespace (x:) Language Features (Silverlight)](/previous-versions/windows/silverlight/dotnet-windows-silverlight/cc188995(v=vs.95)).</span></span>

## <a name="see-also"></a><span data-ttu-id="caac5-158">请参阅</span><span class="sxs-lookup"><span data-stu-id="caac5-158">See also</span></span>

- <xref:System.Windows.FrameworkElement.Name%2A?displayProperty=nameWithType>
- <xref:System.Windows.FrameworkContentElement.Name%2A?displayProperty=nameWithType>
- [<span data-ttu-id="caac5-159">WPF 中的树</span><span class="sxs-lookup"><span data-stu-id="caac5-159">Trees in WPF</span></span>](/dotnet/desktop/wpf/advanced/trees-in-wpf)
